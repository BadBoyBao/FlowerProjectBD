<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xpxp.mapper.InfoMapper">
    <resultMap id="UsernameOnlyMap" type="com.xpxp.entity.Users">
        <result property="username" column="username"/>
    </resultMap>


    <update id="updateInfo">
        update users
        <set>
            <if test="userInfoUpdateDTO.phone != null">phone = #{userInfoUpdateDTO.phone},</if>
            <if test="userInfoUpdateDTO.nickname != null">nickname = #{userInfoUpdateDTO.nickname},</if>
            <if test="userInfoUpdateDTO.gender != null and userInfoUpdateDTO.gender != '' ">gender =
                #{userInfoUpdateDTO.gender},
            </if>
            <if test="userInfoUpdateDTO.image != null">image = #{userInfoUpdateDTO.image},</if>
        </set>
        where user_id = #{userId}
    </update>


    <select id="getInfo" resultType="com.xpxp.VO.UserInfoVO">
        select username, phone, nickname, gender, image
        from users
        where user_id = #{userId}
    </select>
    <select id="getUserName" resultMap="UsernameOnlyMap">
        select username
        from users
        where username = #{username}
    </select>
    <select id="getByUserIdAndUserName" resultType="com.xpxp.entity.Users">
        select *
        from users
        where user_id = #{userId}
          and username = #{username}
    </select>

    <update id="resetPassword">
        update users
        set password = #{newPassword}
        where user_id = #{userId}
          and username = #{username}
    </update>

</mapper>