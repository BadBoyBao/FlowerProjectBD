<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xpxp.mapper.LoginMapper">

    <update id="updateLoginTime">
        update users
        set last_login_time = #{dateTime}
        where user_id = #{userId}
    </update>

    <select id="login" resultType="com.xpxp.entity.Users">
        select *
        from users
        where username = #{username}
          and password = #{password}
    </select>

    <insert id="register">
        insert into users (username, password, phone, nickname, role, created_at, image, is_shop_owner)
        values (#{username}, #{password}, #{phone}, #{nickname}, #{role}, #{createdAt}, #{image}, #{isShopOwner})
    </insert>
</mapper>